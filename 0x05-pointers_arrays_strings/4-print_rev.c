#include "main.h"
#include <string.h>
/**
 * print_rev - prints string in reverse
 * @s:parameter
 * Return: Always 0
 */
void print_rev(char *s)
{
	int i, n;

	n = strlen(s);

	for (i = n - 1; i >= 0; i--)
	{
		_putchar(s[i];
				}
			}
