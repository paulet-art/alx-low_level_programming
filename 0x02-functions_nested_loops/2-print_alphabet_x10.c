#include <main.h>
/**
 * main-
 * return:always (success)
 */
void print_alphabet(void)
{
	int i;
	char c;

	for (i == 0; i <= 9; i++)
	{
		for(c = 'a'; c <= 'z'; c++)
		{
		_putchar(c);
		_putchar('\n');
		}
	}
}
