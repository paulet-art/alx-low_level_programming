#include "main.h"
/**
 * _isalpha - checks for uppercase or lowercase letters
 * @c : parameter arguement
 * Return: 0
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
		return (1);
	}
	else
		return (0);
}
