#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * _abs(int) -  computes the absolute value of an integer
 * @c: parameter arguement
 * Return: Always 0
 */
int _abs(int)
{
	if (c >= 0 || c == 0)
	{
		return (c);
	}
	ese
		return (c * -1);
}
